export type Photo = { id: string; src: string; title: string; date: string; camera: string; location: string; ratio: 'portrait' | 'landscape' };
export function TimelineGallery({ photos, onSelect }: { photos: Photo[]; onSelect: (p: Photo) => void }) { const groups = [...new Set(photos.map(p => p.date))]; return <div className="timeline">{groups.map(date => <section className="day" key={date}><div className="day-heading"><h2>{date}</h2><span>{photos.filter(p => p.date === date).length} photos</span></div><div className="photo-grid">{photos.filter(p => p.date === date).map(photo => <button className={`photo-card ${photo.ratio}`} key={photo.id} onClick={() => onSelect(photo)}><img src={photo.src} alt={photo.title} /><div className="photo-caption"><strong>{photo.title}</strong><span>{photo.location}</span></div></button>)}</div></section>)}</div>; }
